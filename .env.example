# Copy this file to .env and replace the placeholders locally.
# .env is ignored and must not be committed.
FISH_REMOTE_HOST="<ssh-host>"
FISH_REMOTE_SOURCE_DIR="<remote-source-directory>"
FISH_REMOTE_STACK_DIR="<remote-compose-directory>"
WEB_PORT="8080"
