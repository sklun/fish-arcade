server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    access_log /dev/stdout;
    error_log /dev/stderr warn;

    location = /healthz {
        access_log off;
        default_type text/plain;
        return 200 "ok\n";
    }

    location = /index.html {
        return 302 /games/arrow/;
    }

    location = / {
        return 302 /games/arrow/;
    }

    location = /games/arrow/index.html {
        add_header Cache-Control "no-cache" always;
        try_files $uri =404;
    }

    location /games/arrow/assets/ {
        add_header Cache-Control "public, max-age=31536000, immutable" always;
        try_files $uri =404;
    }

    location /games/arrow/ {
        try_files $uri $uri/ /games/arrow/index.html;
    }
}
