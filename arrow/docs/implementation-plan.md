# Arrow 任务计划

## 当前阶段

阶段 2 进行中：直达关卡、导航图标和暂停层视觉已统一，等待浏览器回归。

## 阶段

| 阶段 | 目标 | 范围 | 依赖 | 验收标准 | 验证命令 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 接入平台关卡入口 | 路由、游戏状态初始化与游戏页 | Web 平台关卡入口契约 | 游戏根路径直接进入允许的关卡，非法或未解锁 `level` 参数安全回退 | Arrow 单元测试与生产构建 | completed |
| 2 | 统一导航操作 | 游戏页、暂停页、结果页及图标 | 阶段 1 | 返回平台行为一致，同语义按钮使用同一 Lucide 图标；暂停层与另一款游戏使用一致的布局 | Arrow 浏览器回归 | in_progress |
| 3 | 同步当前文档 | `arrow.md` 与 E2E | 阶段 2 | 规则说明只描述平台入口后的当前流程 | Markdown 定向检查与 `npm run check` | completed |

## 当前验证

- 单元测试、Vue 类型检查和 Vite 生产构建已在容器中通过。
- 暂停层已与 Find Aemeath 统一居中布局、图标层级、主按钮宽度和次要操作排列；颜色仍使用 Arrow 自己的主题变量。
- 浏览器回归需在配置好的整站入口运行。

## 当前约束

- Arrow 通过 Web 路径 `/games/arrow/` 访问，不单独发布宿主机端口。
- 关卡生成、碰撞和进度规则保持由现有 `src/game/` 与 Pinia store 管理。

## 下一阶段

- 部署整站后运行 Arrow 桌面与移动端浏览器回归。
