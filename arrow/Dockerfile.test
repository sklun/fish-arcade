FROM node:22-alpine

WORKDIR /workspace

# Keep dependency installation in its own cacheable image layer. Source files
# are supplied by the test Compose bind mount at runtime.
COPY package.json package-lock.json ./
RUN npm ci

ENV PATH="/workspace/node_modules/.bin:${PATH}"

CMD ["npm", "run", "test:unit"]
